apiVersion: v1
kind: Service
metadata:
  name: backend-v1
  labels:
    app: backend-v1
spec:
  selector:
    app: backend-v1
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP